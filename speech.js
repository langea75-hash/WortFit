function speakText(textValue,rate=.55){if(!textValue)return;const text=new SpeechSynthesisUtterance(textValue);text.lang="de-DE";text.rate=rate;text.pitch=1;speechSynthesis.cancel();speechSynthesis.speak(text)}function startSpeechRecognition(){const SpeechRecognition=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SpeechRecognition){alert("Dein Browser unterstützt Spracheingabe leider nicht.");return}const recognition=new SpeechRecognition();recognition.lang="de-DE";recognition.interimResults=false;recognition.maxAlternatives=1;recognition.start();recognition.onresult=function(event){document.getElementById("sentence").value=event.results[0][0].transcript};recognition.onerror=function(){alert("Spracheingabe hat nicht funktioniert.")}}

