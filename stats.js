function buildStatsHtml(results){if(!results.length)return "<p>Noch keine Übungen gespeichert.</p>";const today=new Date().toLocaleDateString("de-DE"),todayCount=results.filter(x=>x.day===today).length,sentenceCount=results.filter(x=>x.sentenceDone).length,uniqueWords=new Set(results.map(x=>x.word)).size;let html=`<div class="stat-grid"><div class="stat-box">🧠 <b>${results.length}</b><br>Übungen</div><div class="stat-box">📅 <b>${todayCount}</b><br>Heute</div><div class="stat-box">🗣️ <b>${sentenceCount}</b><br>Sätze</div><div class="stat-box">🔤 <b>${uniqueWords}</b><br>Wörter</div></div><h3>Letzte Übungen</h3>`;results.slice(-10).reverse().forEach(x=>html+=`<div class="list-item"><b>${escapeHtml(x.word)}</b> · ${escapeHtml(x.category)}<br>${escapeHtml(x.date)}<br>${x.sentence?escapeHtml(x.sentence):"Kein Satz"}</div>`);return html}
