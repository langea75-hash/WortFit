function buildStatsHtml(results){if(results.length===0)return"Noch keine Übungen gespeichert.";const total=results.length;const sentenceCount=results.filter(r=>r.sentenceDone).length;const today=new Date().toLocaleDateString("de-DE");const todayCount=results.filter(r=>r.day===today).length;const categoryCount={};results.forEach(r=>{categoryCount[r.category]=(categoryCount[r.category]||0)+1});let html=`<div class="stat-item">🧠 Übungen insgesamt: <b>${total}</b></div><div class="stat-item">📅 Übungen heute: <b>${todayCount}</b></div><div class="stat-item">🗣️ Sätze gebildet: <b>${sentenceCount}</b></div><h3>📂 Kategorien</h3>`;for(const c in categoryCount)html+=`<div class="stat-item">${c}: ${categoryCount[c]}</div>`;html+="<h3>Letzte Übungen</h3>";results.slice(-8).reverse().forEach(r=>{html+=`<div class="stat-item">✅ ${r.date}<br><b>${r.word}</b> (${r.category})<br>${r.sentence||"Kein Satz"}</div>`});return html}

